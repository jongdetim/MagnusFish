#pragma once

#include <array>
#include <iostream>
#include <vector>
#include <sstream>
#include <string>

typedef uint8_t u8;
typedef uint16_t u16;
typedef uint64_t u64;

void	run();
void	printBitboard(u64 bitboard);